# Copy to .env and fill in. Two keys because the evaluator runs on a DIFFERENT
# provider than the generator/iterator (the #1 rule). Change providers in config.py.

# Provider A -- generator + iterator
ZAI_API_KEY=

# Provider B -- evaluator + reevaluator (independent judge)
OPENROUTER_API_KEY=
