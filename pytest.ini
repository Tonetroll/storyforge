[pytest]
testpaths = tests
# The default temp root (AppData\Local\Temp) is not writable in this
# environment; route tmp_path-based fixtures to c:/tmp (a declared, writable
# working dir) so isolated channel workspaces can be scaffolded.
addopts = --basetemp=c:/tmp/pytest-dspy
