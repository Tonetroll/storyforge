"""Pipeline package: separate generator / evaluator / iterator / reevaluator
modules, the orchestration loop, the naming + status protocol, logging, the
router, and the learning loop. Everything stays inside this folder."""
